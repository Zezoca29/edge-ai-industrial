spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5433/edgeai
    username: edgeai
    password: edgeai

logging:
  level:
    com.edgeai: DEBUG
    org.springframework.security: DEBUG
